<configuration>

    <!-- Console‑Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- HTTP‑Wire‑Logger auf WARN setzen -->
    <logger name="org.apache.hc.client5.http.wire" level="WARN"/>
    <logger name="org.apache.hc.client5.http.wire.content" level="WARN"/>

    <!-- Alle anderen Logs ab INFO -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>
